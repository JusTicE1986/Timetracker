<Window x:Class="Timetracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:views="clr-namespace:Timetracker.Views"
        mc:Ignorable="d"
        Title="TimeTracker" Width="1024" Height="768"
        MinWidth="1024" MinHeight="600"
        Background="{StaticResource AppBackgroundBrush}"
        FontFamily="Segoe UI Variable">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Exportleiste -->
            <RowDefinition Height="Auto"/>
            <!-- Eingabe + Monatsinfo -->
            <RowDefinition Height="*"/>
            <!-- Wochenübersicht -->
        </Grid.RowDefinitions>

        <!-- Exportleiste -->
        <views:ExportCard Grid.Row="0" Margin="30 30 30 10"/>

        <!-- Eingabe + Monatsinfo -->
        <Grid Grid.Row="1" Margin="30 0 30 20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <views:ZeitErfassungsControl Grid.Column="0" Margin="0 0 20 0"/>
            <views:MonatsInfoControl Grid.Column="1"/>
        </Grid>

        <!-- Wochenübersicht -->
        <views:WochenUebersichtControl Grid.Row="2" Margin="30 0 30 30"/>

        <!-- Dark Mode ToggleButton -->
        <Button x:Name="ThemeToggleButton"
                Content="🌙 Dark Mode"
                Width="120"
                Height="30"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,10,30,0"
                Click="ThemeToggleButton_Click"
                Panel.ZIndex="999"/>
    </Grid>
</Window>
